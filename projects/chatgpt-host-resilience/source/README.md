# Source requirement

2026-09-19: user requested the Fabushi host extension to keep the system awake while ChatGPT automation is active (screen may turn off/lock), re-sync keep-awake across MV3 service-worker restarts, release it in the independent plugin repository, and bundle the newly released auto-confirm userscript. The paired userscript requirements are tracked in the userscript repository.

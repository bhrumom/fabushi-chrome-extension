# 范围与非目标

In scope: MV3 power permission; system keep-awake while active ChatGPT recovery leases exist; release when no active lease; restart/alarm reconciliation; extension version/release; bundled userscript update.

Non-goals: keeping the display awake; OS-level daemons outside Chrome; bypassing user pause/cancel; changing ChatGPT account/security behavior.

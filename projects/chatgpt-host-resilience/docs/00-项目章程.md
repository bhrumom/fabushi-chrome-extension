# 项目章程

Problem: page JavaScript freezes when the host sleeps, so long-running ChatGPT automation stops after lock/sleep. Objective: move keep-awake ownership to the Chrome host extension and bind it to existing recovery leases. Deliverable: released extension with power permission, lease-aware keep-awake and current userscript bundle.

# 发布迁移与回滚

Increment 0.6.14 -> 0.6.15. Release through existing tag workflow. Rollback trigger: power permission causes runtime errors or lease reconciliation fails. Rollback: install prior v0.6.14 package; keep userscript independent update URL unchanged.
